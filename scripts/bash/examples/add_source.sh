#!/usr/bin/env bash

echo "Hello I am a default script $SOURCE_NODE executing on $TARGET_HOST to notify that $SOURCE_NODE is up and running at $SOURCE_IP."
echo "You can upload a .sh file in the add_source artifact of $SOURCE_NODE to replace me."